

const add =  function addition(a, b) {
	return a + b;
}

const subtract = function subtraction(a, b) {
	return a - b;
}

const multiply = function multiplication(a ,b ){
	return a * b;
}

const divide = function division(a,b) {
		callback();

	return a / b;
}

const operate = function operation(a, b, callback) {
	callback(a,b);
}